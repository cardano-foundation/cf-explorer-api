const jsonData = pm.response.json();

//Compare static value
pm.test("Status code is 200", function () {
    pm.response.to.have.status(200);
});

pm.test("Response time is less than 5000ms", function () {
    pm.expect(pm.response.responseTime).to.be.below(5000);
});

//Compare data type
pm.test("Check data type of the response", () => {
    let moment = require ('moment');
    pm.expect(jsonData).to.be.an("object");
    pm.expect(jsonData.isRegistration).to.be.a("boolean");
    pm.expect(jsonData.isUpdate).to.be.a("boolean");
    pm.expect(jsonData.isReward).to.be.a("boolean");
    pm.expect(jsonData.isDeRegistration).to.be.a("boolean");
});

